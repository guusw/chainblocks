#!/bin/bash

source $(dirname ${BASH_SOURCE[0]})/shared

cmake --build ${CHAINBLOCKS_BUILD_PATH} --target cargo-chainblocks
cmake --build ${CHAINBLOCKS_BUILD_PATH} --target chainblocks-tests-runtime
cmake --build ${CHAINBLOCKS_BUILD_PATH} --target cbl
